import { test, expect } from "@playwright/test";

test.describe("Navigation — Pulse Redesign", () => {
  test("sidebar renders with correct nav groups", async ({ page }) => {
    // TODO: implement after Playwright setup
    test.skip();
  });

  test("sidebar collapses to icon-only mode", async ({ page }) => {
    test.skip();
  });

  test("command palette opens with Cmd+K", async ({ page }) => {
    test.skip();
  });
});
