{
  "name": "@ironpulse/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:seed": "prisma db seed",
    "db:seed:dev": "tsx seeds/seed-dev.ts",
    "db:studio": "prisma studio",
    "lint": "tsc --noEmit && eslint src/",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.3",
    "bcryptjs": "^2.4"
  },
  "prisma": {
    "seed": "tsx seeds/seed.ts"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4",
    "prisma": "^6.3",
    "tsx": "^4.21.0",
    "typescript": "^5.7"
  }
}
